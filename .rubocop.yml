inherit_gem:
  rubocop-legion: config/lex.yml

# Actors define `def time` as an instance method; cop cannot see DSL-style time
Legion/Extension/EveryActorRequiresTime:
  Enabled: false

# Private helper methods in runners return nil/numeric guards, not Hash
Legion/Extension/RunnerReturnHash:
  Enabled: false
